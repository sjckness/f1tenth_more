# NOT CURRENTLY LOADED BY ANYTHING (component-auto-start pass): llm.launch.py
# stopped launching llm_planner_node as a Node action at all -- see that file's
# own module docstring for why (a CLI tool with a stdin/argparse mismatch under
# `ros2 launch`, made into a live problem once component_supervisor_node's new
# 'intelligence' component could have auto-started it). interrogations.yaml's
# `planner` entry still names this file as its params_file, but _launch_setup()
# no longer reads that field for anything -- kept as schema/documentation of
# what a future, deliberately-reintroduced node-auto-launch would use, not as
# live config. Left in place (empty, harmless) rather than deleted outright.
#
# Original reasoning, for whenever this DOES get reconnected: llm_url/
# llm_timeout_sec (the two ROS params this node declares) were deliberately never
# set here -- "one source of truth, not duplicated" -- llm_url was built from
# models.yaml's own resolved port and injected as a launch-time override instead
# of being hardcoded in this file. Command text, --dry-run, --confirm all stay
# argparse CLI args regardless, never ROS parameters.
/**:
  ros__parameters: {}
